;;; config.el -*- lexical-binding: t; -*-

(setq user-full-name "copperplate"
      user-mail-address "copperplate@localhost")

(setq doom-theme 'doom-one
      display-line-numbers-type 'relative)

(setq org-directory "~/org/")
